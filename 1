import numpy as np

arr = np.random.randint(0, 10, size=(3, 4))
print(f"原始数组 (3,4):\n{arr}")

reshaped_arr = arr.reshape(4, 3).T
print(f"\n重塑转置后 (3,4):\n{reshaped_arr}")

filtered_arr = arr[arr > 5]
print(f"\n大于5的元素组成的新数组:\n{filtered_arr}")
